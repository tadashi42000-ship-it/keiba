# APP Functions Manual

## 概要

このアプリは、`フェブラリーステークス 2026` を対象にした **競馬情報の収集・整理・可視化アプリ** です。  
`Streamlit` で構築されており、出走馬データ（CSV）をベースに、Web記事・YouTube・アップロード資料を解析して、馬ごとの評価（メリット / デメリット）を表示します。

この説明書は、別ドメイン・別レース・別業務への応用を想定して、機能構成とデータフローを整理したものです。

---

## 主な機能（全体）

1. パスワード認証
2. 出馬表表示（枠番・馬番・騎手・調教師・オッズ）
3. 最新オッズ取得（netkeiba + Playwright / フォールバックあり）
4. Web一括検索（Tavily優先、Gemini検索フォールバック）
5. GeminiによるWeb記事解析（馬別プラス/マイナス抽出）
6. PDF/TXTアップロード解析（レース特徴 / 馬別情報）
7. 馬別総合予想の集計表示
8. レース特徴・傾向の表示
9. YouTube候補検索 + 動画ごとの個別解析

---

## 画面構成（タブ）

### 1. `📋 出馬表`
- 出走予定馬一覧を表形式で表示
- 枠色・オッズバッジ付きの見やすいHTMLテーブル
- `🔄 最新オッズを取得` ボタンで最新オッズ取得
- 取消馬（馬番なし）は表示除外

### 2. `📥 情報入力`
- `🔍 Web 一括検索`
  - Web記事上限数を指定して情報収集
  - Tavily検索を優先
  - 失敗時はGemini検索へ自動フォールバック
- ドキュメントアップロード（PDF / TXT）
  - レース特徴抽出
  - 馬別情報抽出（総合予想に統合）

### 3. `🏇 総合予想（馬別）`
- Web / ドキュメント /（必要に応じてYouTube）由来の情報を馬別に集計
- 各馬ごとにメリット・デメリット・出典を表示
- 取消馬は表示除外
- CSVダウンロード対応

### 4. `🏟️ レース特徴・傾向`
- コース特徴
- 勝ちやすい馬 / 苦手な馬
- 枠順有利 / 不利
- 過去傾向・注目ポイント

### 5. `YOUTUBEから情報入手`
- YouTube検索（候補一覧表示のみ）
- 各動画ごとに `読み込み+概要取得` ボタンで個別解析
- Geminiで馬別のプラス/マイナスを抽出

---

## データソースと役割

### 1. CSV（ローカル）
- ファイル: `february_s_info.csv`
- 用途:
  - 出走馬一覧のベースデータ
  - 馬名リストの基準
  - 枠番/馬番/騎手/調教師/初期オッズの表示

### 2. netkeiba（Webスクレイピング）
- 用途:
  - 最新オッズ、枠番、馬番の取得
- 実装:
  - `Playwright`（優先）
  - 失敗時は別プロセスPlaywright / `requests` フォールバック

### 3. Web記事検索
- Tavily API（優先）
- Gemini Google Search grounding（フォールバック）

### 4. Gemini API
- Web記事スニペットの馬別解析
- YouTube動画説明・字幕の解析
- ドキュメントの馬別情報 / レース特徴抽出

### 5. YouTube Data API
- YouTube動画候補の検索

---

## Web一括検索の処理フロー（現在の構成）

1. 情報入力タブで `Web記事上限数` を指定
2. クエリ群を生成（固定クエリ + 馬名グループクエリ）
3. 各クエリごとに検索実行
   - まず Tavily
   - 失敗 / 未設定 / 空結果なら Gemini検索
4. 記事メタデータを収集（記事上限数まで）
5. 各クエリの代表記事（先頭1件）をGemini解析
6. 馬別のプラス/マイナス情報を抽出
7. `aggregate_horse_analysis()` で馬別集計
8. 取消馬を除外して総合予想に反映

---

## 主要な内部データ（Session State）

- `horse_df`
  - 総合予想（馬別）の表示用DataFrame
- `web_articles`
  - 取得したWeb記事メタデータ一覧
- `web_raw`
  - Gemini解析後の生データ（馬名 / プラス / マイナス / 出典）
- `doc_horse_raw`
  - アップロード資料から抽出した馬別情報
- `race_characteristics`
  - レース特徴タブの表示データ
- `latest_odds`
  - 出馬表に反映する最新オッズ
- `latest_odds_error`
  - オッズ取得失敗時の詳細ログ（表示用）

---

## 外部API / Secrets 設定

### 必須（運用内容による）
- `PASSWORD`
- `GEMINI_API_KEY`

### 任意（機能に応じて）
- `YOUTUBE_API_KEY`（YouTube検索を使う場合）
- `TAVILY_API_KEY`（Tavily検索を使う場合）

### Streamlit Community Cloud の Secrets 例
```toml
PASSWORD = "7777"
GEMINI_API_KEY = "..."
YOUTUBE_API_KEY = "..."
TAVILY_API_KEY = "..."
```

---

## エラーハンドリングの特徴

### オッズ取得
- 失敗時に詳細メッセージをUIへ表示
- 一時的な通信/描画失敗を考慮してリトライあり

### Web検索
- Tavilyが失敗してもGemini検索へフォールバック
- APIキー未設定時も処理中断ではなくフォールバック可能（Geminiが有効なら継続）

### 解析結果の揺れ
- Geminiの返却値が `str` 以外（`list`, `dict`, `None`）でも落ちないように正規化

---

## 取消馬の扱い（重要）

現在は以下のルールで取消馬を非表示にしています。

- `馬番` が1頭でも確定している場合
  - `馬番` 欠損行は「取消/非出走」とみなし除外
- `馬番` が全頭未確定の場合
  - 除外しない（出走確定前とみなす）

反映箇所:
- 出馬表タブ
- 総合予想（馬別）タブ

---

## このアプリを別用途へ応用する際のポイント

### 使い回しやすい部分
- Streamlitタブ構成
- Web検索 + LLM解析 + 集約表示の流れ
- セッションステートによる段階的処理
- フォールバック付き外部API連携

### レース/ドメイン固有で差し替える部分
- `CSV_FILE`, `RACE_ID`, `RACE_URL`
- 固定クエリ群（Web検索クエリ）
- 馬名正規化辞書（`HORSE_NAME_ALIASES`）
- `FEATURED_HORSES`
- レース特徴の初期データ（`RACE_INFO_FROM_DOC`）

### 別業務への転用例
- 株/暗号資産の銘柄別ニュース評価
- 商品レビューの長所/短所集約
- 採用候補者の面談メモ集約
- 競合分析（サービスごとのメリット/デメリット収集）

---

## 既知の運用注意点

1. Streamlit Cloud では `.env` は使われない
- Cloudでは `Secrets` に設定が必要

2. 無料枠アプリはスリープする
- 一定時間アクセスがないとスリープ
- 再起動時にキャッシュが消える

3. Tavily / Gemini のAPI利用量管理が必要
- Web記事上限数を上げるほど時間/コスト増

4. スクレイピング先のHTML変更に弱い
- netkeibaのHTML変更時はオッズ取得ロジック修正が必要

---

## 推奨の拡張案（今後）

1. Web検索ソース比率の可視化（Tavily / Gemini fallback件数）
2. API呼び出し回数カウンタ（Tavily節約用）
3. 解析結果キャッシュの永続化（SQLiteなど）
4. 総合予想スコアリング（メリット・デメリット数の自動点数化）
5. レースごとの設定切替（複数レース対応）

